import { ReactNode } from "react";

export const metadata = {
  title: "Manager | CodeReviewX",
  description: "Manager your Employees",
};

export default function LoginLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
