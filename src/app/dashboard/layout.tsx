import { ReactNode } from "react";

export const metadata = {
  title: "Dashboard | CodeReviewX",
  description: "Manager your Code",
};

export default function LoginLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
